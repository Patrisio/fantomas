import styled from 'styled-components';

export const ViewportPreviewerWrapper = styled.div`
    width: 100%;
    height: calc(100vh - 15px);
    overflow-y: hidden;
    box-shadow: 0 0 25px rgba(0,0,0,.11);
    margin: 15px 15px 0 0;
`;
